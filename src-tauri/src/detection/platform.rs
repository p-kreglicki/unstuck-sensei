use tauri::{AppHandle, Wry};

pub fn setup(_app: &AppHandle<Wry>) {
    // Phase 2B wires the macOS observers and idle polling. Phase 2A keeps the
    // module boundary in place so commands and state can compile first.
}
